{% extends '::admin.html.twig' %}

{% block content %}

    <div class="container">
        <h4>Omgeving aanpassen</h4>

        {{ form_start(form) }}
            {{ form_widget(form) }}
            <input type="submit" class="btn btn-primary" value="opslaan" />
            <a href="{{ path('admin_city_list') }}" class="btn btn-danger">annuleren</a>
        {{ form_end(form) }}
    </div>
{% endblock %}