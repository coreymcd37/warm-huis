{% extends '::admin.html.twig' %}

{% block content %}

    <div class="container">

        <h1>Pagina bekijken <small>{{ content.label }}</small></h1>
        <div>
            {{ content.value | raw }}
        </div>

        <a href="{{ path('admin_content', {'type': 'generic'}) }}" class="btn btn-danger">annuleren</a>
    </div>

{% endblock %}
